# 🐳 Docker 部署

本项目现在支持 Docker 容器化部署！

## 🚀 快速开始

```bash
# 1. 配置环境变量
cp .env.production.example .env.production
# 编辑 .env.production 配置你的 PostgreSQL 连接

# 2. 启动应用
docker-compose up -d

# 3. 验证部署
curl http://localhost:3000/api/health
```

## 📁 新增文件

- `Dockerfile` - 多阶段容器构建配置
- `docker-compose.yml` - 生产环境编排配置
- `.dockerignore` - 构建优化排除文件
- `.env.production.example` - 环境变量配置模板
- `src/app/api/health/route.ts` - 健康检查端点
- `scripts/docker-test.sh` - Docker 测试脚本
- `docs/DOCKER_DEPLOYMENT.md` - 详细部署文档

## 📋 功能特性

✅ **多阶段构建** - 优化的生产镜像（< 500MB）
✅ **健康检查** - 自动监控应用和数据库状态
✅ **环境变量配置** - 支持外部 PostgreSQL
✅ **安全配置** - 非 root 用户运行，资源限制
✅ **云平台就绪** - 支持 AWS ECS、Google Cloud Run、Azure
✅ **测试脚本** - 自动化部署验证

## 🔗 相关文档

- [详细部署指南](docs/DOCKER_DEPLOYMENT.md)
- [健康检查 API](http://localhost:3000/api/health)

## ⚠️ 重要提醒

- **外部服务**: 本配置使用外部 PostgreSQL，不包含容器化数据库
- **环境变量**: 必须配置正确的数据库连接信息
- **端口**: 默认使用 3000 端口，可通过环境变量修改

---

有问题请查看 [部署文档](docs/DOCKER_DEPLOYMENT.md) 或联系技术支持。
